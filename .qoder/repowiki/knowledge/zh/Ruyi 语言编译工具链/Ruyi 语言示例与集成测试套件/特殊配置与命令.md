- 前置要求：执行 `cargo build --release` 生成 `./target/release/ruyic`。
- 运行示例：`./examples/run_examples.sh`。
- 验证输出：`./examples/run_examples.sh --verify` 比对实际输出与 `.expected` 文件。
- 更新基线：`./examples/run_examples.sh --update` 刷新预期输出。