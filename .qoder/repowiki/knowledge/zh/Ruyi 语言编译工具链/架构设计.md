- **工作空间编排**：采用 `Cargo workspace` (resolver 2) 管理 `ruyic`（编译器）与 `ruyi_runtime`（运行时）的依赖同步与版本一致性。
- **分层构建策略**：利用 Makefile 封装差异化构建目标，支持无 LLVM 依赖的运行时快速检查 (`check-runtime`) 及全量发布构建 (`build-release`)。
- **跨模块契约**：编译器通过 C-ABI 与运行时库交互，并依赖标准库（`.ry` 源文件）完成基础语义链接，形成“前端驱动 + 后端生成 + 运行时支撑”的闭环。