- 采用分层设计：`codes.rs` 定义错误分类（词法、语法、类型等）与唯一编码，`render.rs` 负责构建诊断对象并输出带颜色的终端报告。
- `ErrorCode` 作为核心标识，通过 `to_error_code` 方法将各子系统的枚举映射为统一格式。
- `DiagnosticRenderer` 基于 `Write` trait 实现通用输出，配合 `ConsoleFormatter` 处理 ANSI 转义序列以支持跨平台着色。
- `mod.rs` 作为入口点，通过通配符导出所有公共接口，简化外部调用方的引用路径。