- 使用 #[repr(u16)] 显式指定错误码枚举的底层类型，确保数值与文档定义的编号严格一致。
- 在 `render.rs` 中广泛采用 Builder 模式（如 `with_code`, `at`, `with_context`），通过链式调用逐步构建不可变的诊断对象。
- 错误码遵循严格的分类编号规范：E1xxx 为词法错误，E2xxx 为语法错误，E3xxx 为类型错误，E4xxx 为解析错误，W1xxx 为警告。