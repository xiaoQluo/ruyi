- 模块入口 `mod.rs` 统一导出 `Exception` 结构体与 `allocate` 函数，确立对外接口边界。
- `alloc.rs` 封装 unsafe 内存操作（alloc/dealloc/realloc），直接依赖 `std::alloc` 与 `std::ptr`，实现手动内存管理原语。
- `exception.rs` 定义轻量级异常结构，通过 `panic!` 宏实现错误终止，提供 `throw` 便捷函数以简化调用流程。
- 依赖方向清晰：上层业务通过 `ruyic::runtime` 访问底层能力，内部子模块无相互依赖，保持正交性。