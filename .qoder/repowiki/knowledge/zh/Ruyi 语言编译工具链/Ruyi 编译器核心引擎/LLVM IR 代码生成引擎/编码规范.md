- 所有编译子函数（如 `compile_expr`）均接收 `&mut CodegenContext` 并返回 `Result<T, String>`，错误信息通常以 "codegen error" 为前缀。
- 在处理可能包含堆分配对象的局部变量时，普遍使用 `unsafe { ctx.gc_root_scope() }` 创建 RAII 守卫，以确保作用域退出时自动弹出 GC Root 栈帧。
- 对于类方法或泛型实例化，采用名称修饰（Name Mangling）策略生成唯一的 LLVM 函数符号（如 `ClassName_methodName` 或 `func__type`）。