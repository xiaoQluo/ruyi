- 在 `types.rs` 中为复杂类型变体（如 `Function`, `Generic`）使用具名字段的结构体语法以增强可读性。
- 在 `inference.rs` 中广泛采用双向推断模式，根据上下文在“综合”（synthesize）和“检查”（check）模式间切换。
- 使用 `DiagnosticBag` 集中收集编译期诊断信息，并通过 `Severity` 枚举区分错误、警告和提示。
- 在 `environment.rs` 中通过 `push_scope`/`pop_scope` 显式管理词法作用域，确保变量绑定的正确生命周期。