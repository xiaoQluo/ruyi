- 模块采用分层设计：`mod.rs` 定义 `MacroRegistry` 注册表和统一入口 `expand_macros`；`expand.rs` 实现核心递归展开逻辑，支持 AST 遍历与 Token 转换；`pattern.rs` 负责宏规则的解析与参数捕获；`hygiene.rs` 提供基于 `SyntaxContext` 的标识符卫生性隔离机制；`builtins.rs` 注册如 `todo`、`stringify` 等内置宏。
- 依赖方向明确：展开器依赖模式匹配器进行规则比对，依赖卫生性上下文处理标识符冲突，最后通过重新解析 Token 流生成扩展后的 AST。