- 采用递归下降算法结合Pratt解析器（`parse_expr_bp`）处理表达式优先级与结合性，通过`infix_binding_power`定义算子权重。
- 内部划分为四个核心单元：`parser.rs`实现状态机与解析逻辑，`ast.rs`定义全量语法节点结构，`error.rs`基于`thiserror`封装带行列信息的错误类型，`mod.rs`负责模块导出。
- 解析入口`Parser::new`隐式依赖并调用`crate::lexer::Scanner`完成源码到Token流的预处理，形成“扫描-解析”的流水线架构。