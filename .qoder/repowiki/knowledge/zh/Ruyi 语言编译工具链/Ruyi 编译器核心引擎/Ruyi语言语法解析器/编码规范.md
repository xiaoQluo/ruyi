- 在解析关键字或标识符失败时，统一构造包含当前`Location`行列信息的`ParseError::ExpectedToken`以提供精确报错。
- AST节点定义广泛采用包含元数据（如`is_async`、`is_static`、`annotations`）的结构体变体，以保留源码中的修饰符信息。
- 解析复合结构（如函数体、类成员、数组元素）时，普遍采用`while !check(&Token::RBrace)`循环配合`advance`消费Token的模式。