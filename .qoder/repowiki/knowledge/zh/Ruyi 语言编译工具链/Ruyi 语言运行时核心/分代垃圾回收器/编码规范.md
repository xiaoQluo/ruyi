- 所有 GC 对象指针操作均基于负载指针（payload pointer），并通过 `GcObjectHeader::from_payload` 计算头部地址以访问元数据。
- 内部状态管理（如对象列表、根集合）普遍采用 `Mutex<Vec<*mut GcObjectHeader>>` 模式，确保在多线程环境下的内存安全。
- 对象追踪依赖于 `TypeInfo` 中的 `trace_fn` 回调函数，通过迭代器模式递归访问对象内部的子引用。