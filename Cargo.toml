[workspace]
resolver = "2"
members = [
    "crates/ruyic",
    "crates/ruyi_exception",
    "crates/ruyi_runtime",
    "tools/audit-stdlib",
]

[workspace.package]
version = "0.5.11"
edition = "2021"
authors = ["Ruyi Team"]
license = "MIT OR Apache-2.0"

[workspace.dependencies]
# LLVM binding via inkwell (supports LLVM 11-21.1)
inkwell = { version = "0.8", features = ["llvm20-1"] }

# Shared exception-handling helpers for compiler and runtime
ruyi_exception = { path = "crates/ruyi_exception", default-features = false }

# CLI argument parsing
clap = { version = "4.4", features = ["derive", "cargo"] }

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# Logging
log = "0.4"
env_logger = "0.11"

# Benchmarking
criterion = "0.5"

# Dependencies for both crates
[profile.dev]
opt-level = 0
debug = true

[profile.release]
opt-level = 3
lto = false
codegen-units = 16