class Point {
    x: int;
    y: int;
    fn new(x: int, y: int) {
        self.x = x;
        self.y = y;
    }
}

let p: Point = Point.new(3, 4);
print(p.x);
print(p.y);
print(p?.x);
print(p["y"]);

let null_p: Point? = null;
print(null_p?.x);
