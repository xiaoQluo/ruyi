// object_literal.ry — {k:v} literal + bracket access
let point: { x: int, y: int } = { x: 10, y: 20 };
print(point.x);
print(point.y);
print(point["x"]);
print(point["y"]);

let mutable: { n: int } = { n: 1 };
mutable.n = 5;
print(mutable.n);