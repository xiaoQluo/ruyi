[package]
name = "ruyi_exception"
version = "0.5.8"
edition = "2021"
authors = ["Ruyi Team"]
license = "MIT OR Apache-2.0"
description = "Shared exception-handling helpers for the Ruyi compiler and runtime"

[dependencies]
inkwell = { workspace = true, features = ["llvm20-1"], optional = true }

[features]
default = []
llvm20 = ["dep:inkwell"]
