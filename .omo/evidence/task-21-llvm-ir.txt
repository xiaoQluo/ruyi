Task 21 Evidence: LLVM IR Output

Sample LLVM IR output from --emit-llvm for test program:

fn main() { let x = 42; print(x); }

; ModuleID = 'test'
source_filename = "test"

@fmt_unknown = private unnamed_addr constant [11 x i8] c"<unknown>\0A\00", align 1

declare i32 @printf(i8*, ...)

declare i8* @klang_gc_alloc(i64)

declare void @klang_gc_collect()

declare void @klang_throw(i8*)

declare i8* @klang_begin_catch(i8*)

declare void @klang_end_catch()

define i32 @main() {
entry:
  ret i32 0
}

define void @main.1() {
entry:
  %x = alloca { i64, i8* }, align 8
  store i64 42, { i64, i8* }* %x, align 4
  %x1 = load { i64, i8* }, { i64, i8* }* %x, align 8
  %print_unknown = call i32 (i8*, ...) @printf(i8* getelementptr inbounds ([11 x i8], [11 x i8]* @fmt_unknown, i32 0, i32 0))
  ret void
}