# Task 12: Thread-Local GC Heap - Implementation Evidence

## grep 'thread_local' crates/ruyi_runtime/src/gc_exports.rs
Line 12: thread_local! {
    static CURRENT_COLLECTOR: RefCell<GenerationalCollector> =
        RefCell::new(GenerationalCollector::new());

## Key Changes in gc_exports.rs

1. Replaced GLOBAL_COLLECTOR (Mutex<SendCollector>) with thread_local RefCell<GenerationalCollector>
2. All GC functions (ruyi_gc_alloc, ruyi_gc_collect, ruyi_gc_add_root, ruyi_gc_remove_root, ruyi_gc_write_barrier) now use CURRENT_COLLECTOR.with(|collector| ...)

## Implementation Summary

thread_local! {
    static CURRENT_COLLECTOR: RefCell<GenerationalCollector> =
        RefCell::new(GenerationalCollector::new());
}

Each thread gets its own GenerationalCollector instance. When thread exits, the RefCell and GenerationalCollector are automatically dropped (cleanup).

ruyi_gc_alloc: Uses CURRENT_COLLECTOR.with(|collector| collector.borrow_mut().allocate(...))
ruyi_gc_collect: Uses CURRENT_COLLECTOR.with(|collector| collector.borrow_mut().collect_full())
All other functions similarly use the thread-local collector.

✓ Thread-local implementation complete
