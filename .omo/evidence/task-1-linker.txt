Task 1: Fix platform-specific C++ runtime linker
=================================================

Date: 2026-05-04
Branch: dev/v0.5

Change made:
- File: crates/ruyic/src/codegen/generator.rs
- Function: compile_to_binary_with_opt
- Lines: 321-325 (added after .arg("-lm"))

Added platform-specific C++ runtime linker flag:
  .arg(if std::env::consts::OS == "macos" {
      "-lc++"
  } else {
      "-lstdc++"
  })

Purpose: Fix "undefined symbol: __gxx_personality_v0" when linking
binaries that use try/catch exception handling.

Verification:
- Pattern confirmed valid: standalone Rust test compiled and ran correctly
- macOS detected correctly, flag "-lc++" selected (running on macOS)
- Full cargo check requires LLVM 14 which is not available in this environment
- The change uses only std::env::consts::OS (standard library, no external deps)
