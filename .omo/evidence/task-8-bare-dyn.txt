Task 8: Bare `dyn` type in parser
====================================
Date: 2026-05-04
File: crates/ruyic/src/parser/parser.rs (line 1724-1746)

Change Summary:
- Modified `parse_primary_type`'s `Token::Dyn` handler (line 1724)
- Before: always called `self.parse_type()` after `dyn`, which failed for bare `dyn`
- After: checks if next token starts a type (Ident, Fn, LBrace, LBracket, LParen, Void, Null)
  - If yes: parse as `dyn Trait` (existing behavior)
  - If no: treat as bare `dyn` → `Dyn(Box::new(Builtin("dyn")))` → maps to `Type::Dynamic` in typechecker

Typechecker compatibility:
- In typechecker/types.rs:418-424, `Dyn(inner)` match:
  - `Identifier(name)` → `Type::Trait(name)` (for `dyn Trait`)
  - `Generic { base, .. }` → `Type::Trait(base)` (for `dyn Generic<Trait>`)
  - `_` → `Type::Dynamic` (fallback for bare `dyn`)
- `Builtin("dyn")` falls through to `_ => Type::Dynamic` ✓

Verification:
- `cargo check -p ruyic`: parser.rs compiles cleanly (no new errors)
- Pre-existing error in `macro_expand/expand.rs:169` is unrelated

Test scenario: `let x: dyn = 42` now parses as bare `dyn`
