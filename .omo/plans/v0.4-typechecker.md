# v0.4 Typechecker Hardening ✅ DONE

> **Status**: COMPLETED | **Date**: 2026-05-04
> 
> 6/6 features implemented: trait bounds ✅, supertraits ✅, impl resolution ✅, type narrowing ✅, exhaustiveness ✅, self-referential types ✅

## TL;DR

> **Quick Summary**: Fix all stub/partial implementations in the typechecker to enforce trait bounds, validate supertraits, resolve impl methods, narrow types via instanceof/typeof, check match exhaustiveness for named types, and detect self-referential class fields.
> 
> **Deliverables**:
> - `check_bounds()` wired to `TraitRegistry` (no longer unconditional `true`)
> - `extends` clause parsed and supertraits validated transitively
> - `TraitRegistry` used during inference for method resolution on impl types
> - `narrow_for_condition()` handles `instanceof` and `typeof` operators
> - `find_missing_cases()` handles `Type::Named`, `Pattern::Or`, array patterns
> - Self-referential class field detection with cycle reporting
> 
> **Estimated Effort**: Medium
> **Parallel Execution**: YES - 2 waves
> **Critical Path**: Task 1 → Task 2 → Task 5 → Task 6 (bounds needs registry; registry needs impls; narrowing needs bounds; self-ref needs narrowing)

---

## Context

### Original Request
继续 v0.4 版本开发，根据路线图完成类型检查器强化（Typechecker Hardening）

### Interview Summary
**Key Discussions**:
- TDD: Write failing tests first, then implement (RED-GREEN-REFACTOR)
- Scope: All 6 roadmap tasks (2 P0 + 4 P1)
- Parser modification: Allowed for supertrait `extends` syntax (task 2 only)
- No CI, no coverage tools available; all verification via `cargo test`

**Research Findings**:
- `check_bounds()` at generics.rs:349 is a stub returning `true` — needs `TraitRegistry.check_bound()`
- `supertraits` field at traits.rs:32 exists but is always empty — needs parser `extends` + population
- `TraitRegistry` is built in `build_trait_registry()` (traits.rs:230) but not passed to `TypeInference`
- `narrow_for_condition()` at inference.rs:1089 only handles null checks — `BinaryOp::Instanceof`/`Typeof` exist but are ignored
- `find_missing_cases()` at patterns.rs:124 handles bool/null but not `Type::Named`, `Pattern::Or`, arrays
- No self-referential class field detection exists — `occurs_in` only handles `Type::TypeVar`

### Metis Review
Metis did not produce actionable output; gaps identified from explore analysis instead:
- **Guardrails needed**: Task 1 and 3 both touch inference.rs → ensure compatibility
- **Parser mod scope**: Only `extends` clause; no other parser changes
- **Self-referential types**: Need policy decision — reject cycles or warn

---

## Work Objectives

### Core Objective
Complete all 6 typechecker hardening tasks from the v0.4 roadmap: enforce trait bounds, validate supertrait hierarchies, fully resolve impl blocks, narrow types via instanceof/typeof, check match exhaustiveness for all types, and detect self-referential class fields.

### Concrete Deliverables
- `check_bounds()` in generics.rs wired to `TraitRegistry` (returns real results)
- Parser extended to parse `trait Foo extends Bar, Baz` syntax
- `supertraits` populated and validated transitively
- `TypeInference` accepting `TraitRegistry` and using it for impl method resolution
- `narrow_for_condition()` handling `instanceof` and `typeof` binary ops
- `find_missing_cases()` supporting `Type::Named`, `Pattern::Or`, `ArrayPattern`
- Self-referential class field detection emitting diagnostics

### Definition of Done
- [ ] `cargo test -p ruyic --test typechecker` → all new trait bound tests pass
- [ ] `cargo test -p ruyic --test typechecker` → all new supertrait tests pass
- [ ] `cargo test -p ruyic --test typechecker` → all new impl resolution tests pass
- [ ] `cargo test -p ruyic --test typechecker` → all new narrowing tests pass
- [ ] `cargo test -p ruyic --test typechecker` → all new exhaustiveness tests pass
- [ ] `cargo test -p ruyic --test typechecker` → all new self-ref detection tests pass
- [ ] `cargo test --workspace` → no regressions (all 2400+ existing tests pass)

### Must Have
- `check_bounds()` must call `TraitRegistry::check_bound()` for each bound
- Parser must recognize `extends` keyword in trait declarations
- Supertraits must be validated: subtrait implies all supertrait methods
- `TraitRegistry` accessible from `TypeInference` for impl method resolution
- `instanceof` must narrow to class type; `typeof` must narrow to primitive type
- `find_missing_cases` must detect uncovered enum variants for named types
- Own-class field references must produce a diagnostic (error or warning)

### Must NOT Have (Guardrails)
- **No codegen changes** — typechecker only
- **No lexer changes** — keyword additions allowed only if already tokenized
- **No macro_expand changes**
- **No runtime changes**
- **No new external dependencies** (no proptest, insta, mockall)
- **No refactoring existing working typechecker logic** — add, don't rewrite
- **No breaking changes** to existing test suites

---

## Verification Strategy (MANDATORY)

> **ZERO HUMAN INTERVENTION** - ALL verification is agent-executed. No exceptions.

### Test Decision
- **Infrastructure exists**: YES
- **Automated tests**: TDD
- **Framework**: Rust `#[test]` + `cargo test`
- **TDD**: Each task follows RED (failing test) → GREEN (minimal impl) → REFACTOR

### QA Policy
Every task MUST include agent-executed QA scenarios.
Evidence saved to `.sisyphus/evidence/task-{N}-{scenario-slug}.log`.

- **Rust tests**: Use Bash (`cargo test -p ruyic --test typechecker`) — execute, assert pass/fail, capture output
- **Integration**: Use Bash (`cargo test --workspace`) — verify no regressions
- **Manual assertion in code**: Test files with `assert!`, `assert_eq!`, `get_var_type()` helpers

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Start Immediately - foundation + independent tasks):
├── Task 1: TraitRegistry accessible from TypeInference [deep]
├── Task 2: Parser extends clause for supertraits [quick]
├── Task 3: check_bounds() wired to TraitRegistry [deep]
├── Task 4: Supertrait population + validation [unspecified-high]
├── Task 5: Type narrowing instanceof/typeof [unspecified-high]
├── Task 6: Full impl method resolution [deep]
└── Task 7: find_missing_cases for named types, Or, arrays [unspecified-high]

Wave 2 (After Wave 1 - final task + integration):
├── Task 8: Self-referential type detection [deep]
└── Task 9: Integration test suite fixes + regression check [unspecified-high]

Wave FINAL (After ALL tasks — 2 parallel reviews):
├── Task F1: Plan compliance audit [oracle]
├── Task F2: Full regression test run [unspecified-high]
```

Critical Path: Task 1 → Task 3 → Task 6 → Wave 2
Parallel Speedup: ~50% (Wave 1 runs 7 tasks in parallel)

### Agent Dispatch Summary

- **Wave 1**: 7 agents parallel — quick, deep×3, unspecified-high×3
- **Wave 2**: 2 agents parallel — deep, unspecified-high
- **Wave FINAL**: 2 agents parallel — oracle, unspecified-high

---

## TODOs

- [x] 1. **Pass TraitRegistry to TypeInference**

  **What to do**:
  - Add `trait_registry: TraitRegistry` field to `TypeInference` struct in `inference.rs`
  - Add `TraitRegistry` parameter to `TypeInference::new()` constructor
  - Update `TypeChecker::check()` in `checker.rs` to pass `self.trait_registry` to `TypeInference`
  - Store `TraitRegistry` reference in `TypeChecker` and ensure it's populated before inference
  - Write TDD test: create bound check that verifies `check_bounds` is callable from inference context
  - Ensure no compilation errors from the change

  **Must NOT do**:
  - Do not change `TypeInference` method signatures beyond what's needed
  - Do not refactor existing inference logic

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: Structural refactoring across multiple files, requires understanding type systems
  - **Skills**: [`git-master`]
    - `git-master`: For tracking structural changes to existing module boundaries

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 2-7)
  - **Blocks**: Task 3 (check_bounds), Task 6 (impl resolution)
  - **Blocked By**: None (can start immediately)

  **Acceptance Criteria**:
  - [ ] `TypeInference` has `trait_registry` field with type `TraitRegistry`
  - [ ] `TypeChecker::check()` passes `TraitRegistry` to `TypeInference::new()`
  - [ ] `cargo check -p ruyic` compiles without errors
  - [ ] Existing tests pass: `cargo test -p ruyic --test typechecker`

  **QA Scenarios**:
  ```
  Scenario: TypeInference can access TraitRegistry methods
    Tool: Bash
    Steps:
      1. Add a test in typechecker.rs that creates TypeInference with a registry containing a trait
      2. Call trait_registry.trait_names() from within inference
      3. Assert the trait name is accessible
    Expected Result: Registry accessible, test passes
    Evidence: .sisyphus/evidence/task-1-registry-access.log

  Scenario: No regressions in existing tests
    Tool: Bash
    Steps:
      1. Run: cargo test -p ruyic --test typechecker
      2. Check exit code is 0
      3. Count test results → same number as before change
    Expected Result: All existing tests pass, zero failures
    Evidence: .sisyphus/evidence/task-1-regression.log
  ```

  **Commit**: YES
  - Message: `feat(typechecker): pass TraitRegistry to TypeInference`
  - Files: `inference.rs`, `checker.rs`

- [x] 2. **Parser extends clause for supertraits**

  **What to do**:
  - Add `extends` keyword handling in trait declaration parsing (`parser.rs`)
  - Parse `trait Foo extends Bar, Baz { ... }` syntax
  - Store supertrait names in AST `TraitDeclaration` node
  - Add field to `TraitDeclaration` AST type for supertrait list
  - Write TDD test: parse trait with extends clause, verify AST has supertrait names
  - Handle edge cases: single extends, multiple extends, no extends (existing behavior)

  **Must NOT do**:
  - Do not change any other parser productions
  - Do not modify lexer (keyword exists or must be tokenized as ident)

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Well-scoped parser addition following existing patterns
  - **Skills**: [`git-master`]
    - `git-master`: For tracking parser changes

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1, 3-7)
  - **Blocks**: Task 4 (supertrait validation)
  - **Blocked By**: None

  **Acceptance Criteria**:
  - [ ] Parser accepts `trait Foo extends Bar { ... }`
  - [ ] `TraitDeclaration` AST node has `supertraits: Vec<String>` field
  - [ ] Parser test passes: `cargo test -p ruyic --test parser`
  - [ ] No regressions in existing parser tests

  **QA Scenarios**:
  ```
  Scenario: Parse trait with extends clause
    Tool: Bash
    Steps:
      1. Write test in parser.rs: parse "trait Printable extends Debug { fn print(self); }"
      2. Assert AST TraitDeclaration.supertraits == ["Debug"]
    Expected Result: Parser produces correct supertrait list
    Evidence: .sisyphus/evidence/task-2-extends-parse.log

  Scenario: Existing traits without extends still parse
    Tool: Bash
    Steps:
      1. Run: cargo test -p ruyic --test parser
      2. Verify all existing trait declaration tests pass
    Expected Result: No regressions
    Evidence: .sisyphus/evidence/task-2-no-regression.log
  ```

  **Commit**: YES
  - Message: `feat(parser): support trait extends clause for supertraits`
  - Files: `parser.rs`, AST types

- [x] 3. **check_bounds() wired to TraitRegistry**

  **What to do**:
  - Modify `check_bounds()` in `generics.rs:349` to call `trait_registry.check_bound()` per bound
  - Add `trait_registry: &TraitRegistry` parameter to `check_bounds()`
  - For each bound in `param.bounds`, call `trait_registry.check_bound(arg, bound_name)`
  - Keep existing semantics: `dyn` satisfies any bound, empty bounds pass
  - Emit diagnostic when bound check fails
  - Write TDD tests: generic fn with trait bound, pass wrong type → expect error
  - Test edge cases: no bounds, dyn arg, multiple bounds (one fails)

  **Must NOT do**:
  - Do not change `check_bounds` return type or call semantics beyond adding registry param
  - Do not remove the `dyn` and empty-bounds fast paths

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: Type system logic, must understand subtyping and trait resolution
  - **Skills**: [`git-master`]
    - `git-master`: For tracking generics changes

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1, 2, 4-7)
  - **Blocks**: None (endpoint)
  - **Blocked By**: Task 1 (registry must be accessible)

  **Acceptance Criteria**:
  - [ ] `check_bounds()` calls `TraitRegistry::check_bound()` for each bound
  - [ ] Non-trait-implementing arg produces diagnostic error
  - [ ] `dyn` arg passes all bound checks
  - [ ] New tests: `cargo test -p ruyic --test typechecker`

  **QA Scenarios**:
  ```
  Scenario: Type not implementing trait fails bound check
    Tool: Bash
    Steps:
      1. Write test: generic fn requiring Printable, call with int → expect error
      2. Assert check_bounds returns false and diagnostic emitted
    Expected Result: Bound violation caught
    Evidence: .sisyphus/evidence/task-3-bound-fail.log

  Scenario: dyn type passes any bound
    Tool: Bash
    Steps:
      1. Write test: generic fn requiring Printable, call with dyn → check_bounds true
    Expected Result: dyn passes all bounds
    Evidence: .sisyphus/evidence/task-3-dyn-pass.log
  ```

  **Commit**: YES
  - Message: `feat(typechecker): enforce trait bounds via TraitRegistry`
  - Files: `generics.rs`, `typechecker.rs` (tests)

- [x] 4. **Supertrait population + validation**

  **What to do**:
  - In `traits.rs`, modify `register_trait()` to populate `supertraits` from `TraitDeclaration.supertraits`
  - Add `validate_supertraits()` method to `TraitRegistry`:
    - For each subtrait, verify all supertrait methods are implemented (directly or inherited)
    - Check for circular supertrait references (A extends B, B extends A)
    - Validate supertrait transitivity: if A extends B extends C, A must implement C's methods
  - Call `validate_supertraits()` from `build_trait_registry()` or `TypeChecker::check()`
  - Write TDD tests: trait with valid extends, circular extends, missing supertrait method

  **Must NOT do**:
  - Do not change TraitInfo field names or types
  - Do not modify how methods are stored

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Algorithmic validation (transitivity check, cycle detection) needs careful implementation
  - **Skills**: [`git-master`]
    - `git-master`: For tracking trait registry changes

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1-3, 5-7)
  - **Blocks**: Task 6 (impl resolution may reference supertraits)
  - **Blocked By**: Task 2 (needs parsed extends clause)

  **Acceptance Criteria**:
  - [ ] `supertraits` field populated during `register_trait()`
  - [ ] Circular supertrait chains detected and reported
  - [ ] Missing supertrait methods detected
  - [ ] New tests: `cargo test -p ruyic --test typechecker`

  **QA Scenarios**:
  ```
  Scenario: Valid supertrait hierarchy accepted
    Tool: Bash
    Steps:
      1. Write test: trait A extends B, A implements all B methods + its own
      2. Assert validate_supertraits() passes
    Expected Result: No error
    Evidence: .sisyphus/evidence/task-4-valid-hierarchy.log

  Scenario: Circular supertrait detected
    Tool: Bash
    Steps:
      1. Write test: trait A extends B, trait B extends A
      2. Assert validate_supertraits() returns error diagnostic
    Expected Result: Circular dependency error
    Evidence: .sisyphus/evidence/task-4-circular-error.log
  ```

  **Commit**: YES
  - Message: `feat(typechecker): populate and validate supertrait hierarchies`
  - Files: `traits.rs`, `typechecker.rs` (tests)

- [x] 5. **Type narrowing via instanceof and typeof**

  **What to do**:
  - Extend `narrow_for_condition()` in `inference.rs:1089` to handle:
    - `x instanceof ClassName` → narrow `x` to `Type::Named("ClassName")` in true branch
    - `typeof x === "string_literal"` → narrow `x` to corresponding primitive type
   - Handle `BinaryOp::Instanceof` and `UnaryOp::Typeof` (with `BinaryOp::StrictEquals`) cases
  - For `instanceof` false branch: complement type (exclude the class)
  - Support `typeof` with all primitive type literals: `"int"`, `"float"`, `"bool"`, `"string"`, `"bigint"`
  - Write TDD tests: instanceof narrowing, typeof narrowing, false branch handling

  **Must NOT do**:
  - Do not break existing null narrowing logic
  - Do not add narrowing that changes codegen — this is type-level only

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Type narrowing logic with multiple operators, must handle edge cases carefully
  - **Skills**: [`git-master`]
    - `git-master`: For tracking inference changes

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1-4, 6-7)
  - **Blocks**: None
  - **Blocked By**: None (independent — narrowing uses existing Type system)

  **Acceptance Criteria**:
  - [ ] `x instanceof ClassName` narrows type in true branch
  - [ ] `typeof x === "string"` narrows to `Type::String`
  - [ ] False branch type complement works
  - [ ] Existing null narrowing unaffected
  - [ ] New tests: `cargo test -p ruyic --test typechecker`

  **QA Scenarios**:
  ```
  Scenario: instanceof narrows to class type
    Tool: Bash
    Steps:
      1. Write test: if (x instanceof Point) { /* x should be Point here */ }
      2. Assert type of x in true branch is Type::Named("Point")
    Expected Result: Type narrowed to Point
    Evidence: .sisyphus/evidence/task-5-instanceof-narrow.log

  Scenario: typeof narrows to primitive
    Tool: Bash
    Steps:
      1. Write test: if (typeof x === "int") { /* x should be int */ }
      2. Assert type of x in true branch is Type::Int
    Expected Result: Type narrowed to int
    Evidence: .sisyphus/evidence/task-5-typeof-narrow.log
  ```

  **Commit**: YES
  - Message: `feat(typechecker): narrow types via instanceof and typeof`
  - Files: `inference.rs`, `typechecker.rs` (tests)

- [x] 6. **Full impl method resolution via TraitRegistry**

  **What to do**:
  - In `TypeInference`, when encountering a method call on a named type (e.g., `obj.method()`), look up the trait impl in `TraitRegistry`
  - Add `resolve_impl_method(type_name, method_name)` to `TraitRegistry`:
    - For a given type + method name, find which trait impl provides the method
    - Return the trait info and method signature
  - Use resolved method info during type inference to determine return type and parameter types
  - Handle `self` type substitution (map `self` to the implementing type)
  - Write TDD tests: impl method call on named type, method not found error, self-type resolution

  **Must NOT do**:
  - Do not generate code — this is type verification only
  - Do not change how methods are stored in TraitInfo

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: Complex type resolution involving self substitution, cross-module references
  - **Skills**: [`git-master`]
    - `git-master`: For tracking inference + traits changes

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1-5, 7)
  - **Blocks**: None
  - **Blocked By**: Task 1 (registry accessible), Task 4 (supertrait data available)

  **Acceptance Criteria**:
  - [ ] `obj.method()` on type with impl resolves to correct method signature
  - [ ] `self` type correctly substituted with implementing type
  - [ ] Missing method on type with impl produces error
  - [ ] New tests: `cargo test -p ruyic --test typechecker`

  **QA Scenarios**:
  ```
  Scenario: Impl method resolution for named type
    Tool: Bash
    Steps:
      1. Write test: class Point with impl Printable { fn format(self): string { ... } }
      2. Assert p.format() returns string type
    Expected Result: Method resolved, return type is string
    Evidence: .sisyphus/evidence/task-6-impl-resolve.log

  Scenario: Method not found on impl type
    Tool: Bash
    Steps:
      1. Write test: class Point without impl, call p.format()
      2. Assert diagnostic emitted: "method not found"
    Expected Result: Error reported
    Evidence: .sisyphus/evidence/task-6-method-not-found.log
  ```

  **Commit**: YES
  - Message: `feat(typechecker): resolve impl trait methods via TraitRegistry`
  - Files: `traits.rs`, `inference.rs`, `typechecker.rs` (tests)

- [x] 7. **Exhaustiveness checking for named types, Or patterns, arrays**

  **What to do**:
  - Extend `find_missing_cases()` in `patterns.rs:124`:
    - **Named types**: When matching on a named type, check if all known variants are covered
    - **Or patterns** (`Pattern::Or`): Decompose into sub-patterns, check coverage for each
    - **Array patterns**: Handle `[]`, `[x]`, `[x, y]`, `[x, ...rest]` — detect empty array missing
  - Extend `pattern_covered_cases()` to extract case info from Or and Array patterns
  - Update `analyze_patterns()` to pass type information (what type is being matched on)
  - Write TDD tests: exhaustive match on class, non-exhaustive match emits warning, Or pattern coverage, array coverage

  **Must NOT do**:
  - Do not change how `Statement::Match` is type-checked in inference.rs
  - Do not add new diagnostics — use existing `NonExhaustiveMatch`

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Pattern matching algorithm extension, requires understanding of pattern decomposition
  - **Skills**: [`git-master`]
    - `git-master`: For tracking patterns changes

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1-6)
  - **Blocks**: None
  - **Blocked By**: Task 1 (needs type info flow for named types)

  **Acceptance Criteria**:
  - [ ] Named type match detects uncovered variants
  - [ ] Or pattern coverage decomposed correctly
  - [ ] Array pattern detects missing empty case
  - [ ] New tests: `cargo test -p ruyic --test typechecker`

  **QA Scenarios**:
  ```
  Scenario: Non-exhaustive match on named type
    Tool: Bash
    Steps:
      1. Write test: enum Shape { Circle, Square } with match on Shape covering only Circle
      2. Assert NonExhaustiveMatch diagnostic emitted
    Expected Result: Warning for uncovered variant Square
    Evidence: .sisyphus/evidence/task-7-non-exhaustive.log

  Scenario: Exhaustive match with Or pattern
    Tool: Bash
    Steps:
      1. Write test: match on bool with true | false arm (Or pattern)
      2. Assert no exhaustiveness warning
    Expected Result: Clean, no diagnostic
    Evidence: .sisyphus/evidence/task-7-or-exhaustive.log
  ```

  **Commit**: YES
  - Message: `feat(typechecker): exhaustiveness for named types, Or patterns, arrays`
  - Files: `patterns.rs`, `typechecker.rs` (tests)

- [x] 8. **Self-referential class field detection**

  **What to do**:
  - Add a class-definition stack in `TypeInference` to track which class is being defined
  - During field type checking, if a field type references the enclosing class (e.g., `next: ListNode?`), detect this
  - Emit a diagnostic: either warning (for nullable self-refs like `ListNode?`) or error (for non-nullable self-refs)
  - Reuse `occurs_in` pattern from `constraints.rs:266` extended to `Type::Named`
  - Handle transitive self-refs: `class A { b: B } class B { a: A }` → detect cross-reference
  - Write TDD tests: self-referential field (nullable, non-nullable), cross-class cycle

  **Must NOT do**:
  - Do not generate code for self-referential types
  - Do not prevent compilation — emit warning only (let codegen decide if it can handle it)

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: Cycle detection with transitive closure, requires careful constraint tracking
  - **Skills**: [`git-master`]
    - `git-master`: For tracking inference changes

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Task 9 only)
  - **Parallel Group**: Wave 2
  - **Blocks**: None
  - **Blocked By**: Task 1 (needs TypeInference context), Task 5 (may interact with narrowed types)

  **Acceptance Criteria**:
  - [ ] Self-referential field `next: ListNode?` produces warning
  - [ ] Self-referential field `next: ListNode` (non-nullable) produces error
  - [ ] Cross-class reference cycle detected
  - [ ] Normal non-self-referential fields produce no diagnostic
  - [ ] New tests: `cargo test -p ruyic --test typechecker`

  **QA Scenarios**:
  ```
  Scenario: Nullable self-referential field emits warning
    Tool: Bash
    Steps:
      1. Write test: class ListNode { next: ListNode? }
      2. Assert diagnostic is a warning (not error)
    Expected Result: Warning emitted, not error
    Evidence: .sisyphus/evidence/task-8-self-ref-warning.log

  Scenario: Non-nullable self-referential field emits error
    Tool: Bash
    Steps:
      1. Write test: class ListNode { next: ListNode }
      2. Assert diagnostic is an error
    Expected Result: Error emitted
    Evidence: .sisyphus/evidence/task-8-self-ref-error.log

  Scenario: Normal fields emit no diagnostic
    Tool: Bash
    Steps:
      1. Write test: class Point { x: float; y: float }
      2. Assert zero diagnostics
    Expected Result: Clean, no diagnostics
    Evidence: .sisyphus/evidence/task-8-normal-fields.log
  ```

  **Commit**: YES
  - Message: `feat(typechecker): detect self-referential class field types`
  - Files: `inference.rs`, `constraints.rs`, `typechecker.rs` (tests)

- [x] 9. **Integration test suite fixes + regression check**

  **What to do**:
  - Fix any existing `#[ignore]` tests in typechecker.rs that should now pass with v0.4 changes
  - Update integration test runner if needed for new type patterns
  - Run full workspace test suite: `cargo test --workspace`
  - Run clippy: `cargo clippy --workspace`
  - Fix any newly introduced warnings or errors
  - Verify zero regressions from v0.3 baseline

  **Must NOT do**:
  - Do not change test logic for tests that legitimately fail due to other limitations
  - Do not remove `#[ignore]` from tests unrelated to v0.4 changes

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: Integration testing with full workspace, may need to trace failures across modules
  - **Skills**: [`git-master`]
    - `git-master`: For comparing against v0.3 baseline

  **Parallelization**:
  - **Can Run In Parallel**: YES (with Task 8 only)
  - **Parallel Group**: Wave 2
  - **Blocks**: None (final task)
  - **Blocked By**: Tasks 1-7 (all implementation must be done)

  **Acceptance Criteria**:
  - [ ] `cargo test --workspace` passes with zero failures
  - [ ] `cargo clippy --workspace` produces zero new warnings
  - [ ] At least 3 previously `#[ignore]`'d tests now pass and un-ignored

  **QA Scenarios**:
  ```
  Scenario: Full workspace test suite passes
    Tool: Bash
    Steps:
      1. Run: cargo test --workspace 2>&1 | tee test-output.txt
      2. Check exit code = 0
      3. Grep for "FAILED" → 0 results
    Expected Result: All tests pass
    Evidence: .sisyphus/evidence/task-9-regression-pass.log

  Scenario: Clippy clean
    Tool: Bash
    Steps:
      1. Run: cargo clippy --workspace 2>&1
      2. Check exit code = 0
      3. Count warnings → same or fewer than v0.3 baseline
    Expected Result: No new warnings
    Evidence: .sisyphus/evidence/task-9-clippy-clean.log
  ```

  **Commit**: YES
  - Message: `test(typechecker): integration fixes and regression verification`
  - Files: `typechecker.rs`, integration runner (if needed)

---

## Final Verification Wave (MANDATORY — after ALL implementation tasks)

> 2 review agents run in PARALLEL. ALL must APPROVE. Present consolidated results to user and get explicit "okay" before completing.

- [x] F1. **Plan Compliance Audit** — `oracle`
  Read the plan end-to-end. For each "Must Have": verify implementation exists (read file, run cargo test). For each "Must NOT Have": search codebase for forbidden patterns — reject with file:line if found. Check evidence files exist in .sisyphus/evidence/. Compare deliverables against plan.
  Output: `Must Have [N/N] | Must NOT Have [N/N] | Tasks [N/N] | VERDICT: APPROVE/REJECT`

- [x] F2. **Full Regression Test Run** — `unspecified-high`
  Run `cargo test --workspace` and capture full output. Verify ALL existing tests pass (no regressions). Run `cargo clippy --workspace` for lint. Check for `#[ignore]` additions (should be zero). Verify new test files exist and compile.
  Output: `cargo test [PASS/FAIL] | clippy [CLEAN/N warnings] | New tests [N] | Regressions [NONE/N found] | VERDICT`

---

## Commit Strategy

- Each task produces 1 commit: `feat(typechecker): short description`
- Merge all to `dev/v0.4` via PR

---

## Success Criteria

### Verification Commands
```bash
cargo test -p ruyic --test typechecker  # All new + existing typechecker tests
cargo test --workspace                  # Full regression
cargo clippy --workspace                # Lint clean
```

### Final Checklist
- [ ] All "Must Have" present
- [ ] All "Must NOT Have" absent
- [ ] All tests pass
- [ ] No regressions
