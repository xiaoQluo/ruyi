// Async/Await example for Ruyi
async fn delay(ms: int): void {
  // Simulate some async work
  let start = 0;
  let limit = ms * 1000;
  while (start < limit) {
    start = start + 1;
  }
}

async fn computeSquare(n: int): int {
  await delay(10);
  return n * n;
}

async fn main() {
  print("Starting async computations...\n");

  let result1 = await computeSquare(5);
  let result2 = await computeSquare(10);
  let result3 = await computeSquare(15);

  print(result1);
  print(result2);
  print(result3);

  print("All async computations complete!\n");
}